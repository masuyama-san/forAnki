---
type: AnkiCards
title: Dockerfileでビルド時にコマンドを実行する命令は？
date: 2026-02-08
tags:
  - Docker
  - Dockerfile
  - run
id: 1753494305395
---

# Card: Dockerfileでビルド時にコマンドを実行する命令は？

## Question
Dockerfileでビルド時にコマンドを実行する命令は？

## Answer
RUN<br><pre><code>RUN apt-get update &amp;&amp; apt-get install -y python3    # パッケージインストール
RUN pip install flask                               # Pythonパッケージインストール
RUN npm install                                     # Node.jsパッケージインストール</code></pre>
